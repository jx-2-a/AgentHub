[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "agentweb"
version = "0.1.0"
description = "Agent Hub 接入 SDK：BaseSession 契约 + WebSessionClient"
requires-python = ">=3.9"
dependencies = ["aiohttp>=3.9"]

[tool.setuptools]
packages = ["agentweb"]
